{
  "name": "rig",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile rig && bun build --compile src/rigd.ts --outfile rigd",
    "start": "bun run src/index.ts"
  },
  "bin": {
    "rig": "./rig",
    "rigd": "./rigd"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-beta.57",
    "effect": "4.0.0-beta.57"
  }
}
