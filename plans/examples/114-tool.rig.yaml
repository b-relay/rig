# Accepted #114 design. The schema is implemented; tests/config-examples-e2e.test.ts validates this file.
# This Project has no server, port, hostname or proxy.
name: report
production_branch: main
build: go build -o .rig-build/report ./cmd/report

tools:
  report:
    bin: .rig-build/report

targets:
  working:
    name: local
  stable:
    name: live
