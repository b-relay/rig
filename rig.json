{
  "name": "rig",
  "description": "Rig project config for the Rig CLI and daemon binaries.",
  "components": {
    "rig": {
      "mode": "installed",
      "entrypoint": "rig",
      "build": "bun install --frozen-lockfile && bun build --compile src/index.ts --outfile rig"
    },
    "rigd": {
      "mode": "installed",
      "entrypoint": "rigd",
      "build": "bun install --frozen-lockfile && bun build --compile src/rigd.ts --outfile rigd"
    }
  },
  "live": {
    "deployBranch": "main"
  },
  "deployments": {
    "subdomain": "${branchSlug}"
  }
}
