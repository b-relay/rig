{
  "name": "rig",
  "version": "0.1.1",
  "hooks": {
    "preStart": "bun install"
  },
  "environments": {
    "prod": {
      "services": [
        {
          "name": "rig",
          "type": "bin",
          "entrypoint": "rig",
          "build": "bun build --compile src/index.ts --outfile rig"
        }
      ]
    }
  }
}
