{
  "name": "rig",
  "version": "0.7.0",
  "hooks": {
    "preStart": "bun install"
  },
  "environments": {
    "dev": {
      "services": [
        {
          "name": "rig",
          "type": "bin",
          "entrypoint": "rig",
          "build": "bun build --compile src/index.ts --outfile rig"
        }
      ]
    },
    "prod": {
      "deployBranch": "main",
      "services": [
        {
          "name": "rig",
          "type": "bin",
          "entrypoint": "rig",
          "build": "bun build --compile src/index.ts --outfile rig"
        }
      ]
    }
  }
}
