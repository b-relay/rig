{
  "name": "rig",
  "version": "0.4.0",
  "hooks": {
    "preStart": "bun install"
  },
  "environments": {
    "prod": {
      "deployBranch": "main",
      "services": [
        {
          "name": "rig",
          "type": "bin",
          "entrypoint": "rig",
          "build": "bun build --compile src/index.ts --outfile rig"
        }
      ]
    }
  }
}
