{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/b-relay/rig/main/schemas/host-config.schema.json",
  "title": "Rig Host config (config.yaml)",
  "description": "Machine capability in <RIG_ROOT>/config.yaml; every key is optional.",
  "type": "object",
  "properties": {
    "deploy": {
      "description": "Host deployment defaults.",
      "default": {},
      "type": "object",
      "properties": {
        "production_branch": {
          "default": "main",
          "description": "Production branch for a Project whose rig.yaml sets none, and the rig init default when origin/HEAD names no branch.",
          "type": "string",
          "minLength": 1
        },
        "previews": {
          "description": "Preview limit.",
          "default": {},
          "type": "object",
          "properties": {
            "max": {
              "default": 25,
              "description": "Most Previews one Project may have; every recorded Preview counts, running or stopped.",
              "type": "integer",
              "minimum": 1,
              "maximum": 9007199254740991
            },
            "replace_policy": {
              "default": "oldest",
              "description": "At the limit: oldest destroys the oldest Preview to make room (incomplete deploys first, then stopped, then running); reject refuses the deploy.",
              "type": "string",
              "enum": [
                "oldest",
                "reject"
              ]
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "providers": {
      "description": "Provider settings.",
      "default": {},
      "type": "object",
      "properties": {
        "caddy": {
          "description": "Caddy provider settings.",
          "default": {},
          "type": "object",
          "properties": {
            "caddyfile": {
              "description": "File Rig writes its marked route blocks into; defaults to proxy/Caddyfile under the Rig state directory.",
              "type": "string",
              "minLength": 1
            },
            "host_caddyfile": {
              "description": "Caddyfile the running Caddy loads; it must be the route file or import it. Defaults to the first of /usr/local/etc/Caddyfile, /opt/homebrew/etc/Caddyfile, /etc/caddy/Caddyfile that exists.",
              "type": "string",
              "minLength": 1
            },
            "extra_config": {
              "default": [],
              "description": "Extra trusted Caddy directives added inside every site block Rig writes, such as import cloudflare. They may use snippets the Host Caddyfile defines once it imports the route file.",
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            },
            "reload": {
              "description": "Reload behavior.",
              "default": {},
              "type": "object",
              "properties": {
                "mode": {
                  "default": "manual",
                  "description": "manual: Rig writes the route file and you reload Caddy. command: Rig runs the reload command after each route change and restores the previous routes when it fails.",
                  "type": "string",
                  "enum": [
                    "manual",
                    "command"
                  ]
                },
                "command": {
                  "description": "Host Caddy reload command; required when mode is command.",
                  "type": "string",
                  "minLength": 1
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "diagnostics": {
      "description": "Rig Diagnostic log policy.",
      "default": {},
      "type": "object",
      "properties": {
        "retention_days": {
          "default": 14,
          "description": "Days of Diagnostic logs to keep.",
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        },
        "level": {
          "default": "info",
          "description": "Diagnostic log verbosity.",
          "type": "string",
          "enum": [
            "debug",
            "info",
            "warn",
            "error"
          ]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
